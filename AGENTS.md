# AGENTS.md（エージェント作業指示）

このリポジトリは、書籍「AI開発のためのGitHubワークフロー実践ガイド」の公開・保守用です。エージェントが PR を作成する際の手戻りを最小化するため、最低限の前提・禁止事項・検証手順をまとめます。

## 1. 作業の前提

- **作業単位**: Issue に「受入基準・制約・テスト・変更禁止領域」が書かれていることを前提にします（第2章の方針）。
- **ブランチ運用**: `main` へ直接 push せず、作業ブランチ → PR の順で進めます。
- **言語/トーン**: 日本語。ビジネス用途に適した表現を用い、推測で断定しません（不明点は「分かりません」と明示します）。

## 2. 主要ディレクトリ（正本の扱い）

- `docs/`: **GitHub Pages 公開物**。PR の Book QA（Unicode/Textlint/リンク/構造/Jekyll build）も原則ここをスキャンします。
- `src/`: テンプレ由来の**ビルド入力**。`docs/` と完全同期していない場合があります。変更時は `docs/` を正とし、必要に応じて `src/` にも反映してください。
- `examples/`: コピペ導入用のサンプル（テンプレ、チェックリスト等）。
- `.github/`: Issue/PR テンプレ、ワークフロー。

## 3. 変更禁止・注意事項

- **Secrets/個人情報を扱わない**: Issue/PR本文、ログ、コミットにトークン・パスワード等を含めません。
- **自動生成物の手編集を避ける**: `docs/assets/`（CSS/JS/画像最適化物）は、原則として直接編集しません（必要な場合は理由を PR に明記）。
- **差分は最小**: 章間整合を保ちつつ、Issue の受入基準を満たす最小差分で作業します。

## 4. 執筆ルール（本文/付録）

- 見出し番号（例: `## 6.1`）は既存の体系に合わせます。
- 外部仕様（料金、モデル、UI手順など）は変動し得るため、**固定値の断定**を避け、必要に応じて公式ドキュメント参照を併記します。
- 用語は一貫させます（例: 「エージェント」「受入基準」「変更禁止領域」「権限境界」）。

## 5. ローカル検証（推奨）

前提: Node.js `^20.9.0 || >=22`（`node --version` で確認）、Python 3 系。

- `python3 scripts/validate_links.py docs`（内部/外部リンクの存在チェック）
- `npm ci`（初回のみ）
- `npm run lint:light`（`src/` の Markdown lint）
- `npm run test:light`（`lint:light` + `build`）

注: `npm run build` は `docs/` を再生成します。`src/` と `docs/` の同期状況により差分が大きくなる場合があるため、PR では差分の妥当性を必ず確認してください。

## 6. PR 作成時の最低限チェック

- Issue の受入基準を満たしている（満たし方を PR で説明できる）
- 章参照や内部リンクが壊れていない
- 変更範囲が過度に広がっていない
- GitHub Actions の `Book QA` が PASS する
